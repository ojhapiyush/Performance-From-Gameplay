# Performance-From-Gameplay
For each &lt;session_id>_&lt;question #>, model is predicting the correct column, identifying whether the user for this particular session will answer this question correctly, using only the previous information for the session.
